

---

```markdown
<!-- File: plan.md -->
# ASCII動画プレイヤー「asciiplay」開発計画書

## 1. プロジェクト概要

動画ファイルをターミナル上でASCIIアートとして再生できるCLIツール。動画に任意の名前を付けて登録し、`asciiplay play <名前>` のようなコマンドで呼び出して再生できる仕組みを構築する。

## 2. 目的

- 動画をターミナル上で文字表現として楽しめるようにする
- 動画ごとに名前・表示設定（カラー/幅など）を紐付けて管理し、簡単なコマンドで呼び出せるようにする
- 将来的に音声同期再生やプレイリスト機能などへ拡張できる基盤を作る

## 3. 機能要件

### 3.1 必須機能

| 機能 | コマンド例 | 内容 |
|---|---|---|
| 動画登録 | `asciiplay add <name> <path> [--color] [--width N]` | 動画に名前を付けて登録し、表示設定を保存する |
| 動画削除 | `asciiplay remove <name>` | 登録済みの動画情報を削除する |
| 一覧表示 | `asciiplay list` | 登録済み動画の名前・パス・設定を表示する |
| 動画再生 | `asciiplay play <name> [--color] [--width N]` | 登録済み動画をASCIIアートとしてターミナル上に再生する |

### 3.2 表示要件

- 動画フレームをグレースケール化し、明るさに応じてASCII文字（` .:-=+*#%@`）に変換する
- `--color` 指定時は24bitカラー（ANSIエスケープシーケンス）でフルカラー表示する
- ターミナルサイズに応じて自動で表示幅・高さを調整する（`--width` で明示的に上書き可能）
- 元動画のFPSに同期した再生速度で再生する
- `Ctrl+C` で安全に中断でき、カーソル表示状態を必ず元に戻す

### 3.3 任意機能（拡張候補）

- 音声同時再生（`ffmpeg` で音声抽出 + `pygame` で再生）
- 再生中の一時停止・スキップ操作
- 複数動画を順番に再生する `playlist` コマンド
- Braille文字を用いた高解像度表示モード
- 256色モードへの対応（フルカラー非対応ターミナル向け）

## 4. 技術スタック

| 項目 | 内容 |
|---|---|
| 言語 | Python 3.x |
| 動画処理 | OpenCV (`opencv-python`) |
| 数値処理 | NumPy |
| 音声処理（拡張） | ffmpeg（外部コマンド）+ pygame |
| 設定管理 | JSON（`~/.asciiplay/videos.json`） |
| CLI構築 | argparse（サブコマンド構成） |
| パッケージ管理 | pyproject.toml + setuptools |

## 5. ディレクトリ構成

```
asciiplay/
├── asciiplay/
│   ├── __init__.py
│   ├── cli.py        # サブコマンド定義（add/remove/list/play）
│   ├── config.py     # 動画名と設定情報の登録・読み書き
│   └── player.py     # フレーム変換・再生ロジック
└── pyproject.toml
```

## 6. データ設計

登録情報は `~/.asciiplay/videos.json` に以下の形式で保存する。

```json
{
  "mymovie": {
    "path": "/home/user/Videos/movie.mp4",
    "color": true,
    "width": 100
  }
}
```

- `path`: 動画ファイルの絶対パス
- `color`: デフォルトのカラー表示有無
- `width`: デフォルトの出力幅（未指定時はターミナル幅に自動追従）

## 7. 開発フェーズ

### フェーズ1: コア再生機能の実装
- OpenCVで動画を読み込み、フレームをASCII文字に変換する処理を実装する
- ANSIエスケープシーケンスによる上書き描画・FPS同期処理を実装する
- 単体スクリプトとして動作確認する

### フェーズ2: 名前付き登録機能の実装
- `config.py` で動画名と設定情報をJSONに保存・読込する機能を実装する
- `add` / `remove` / `list` コマンドを実装する

### フェーズ3: CLI統合
- `argparse` によるサブコマンド構成（`add`/`remove`/`list`/`play`）を実装する
- `pyproject.toml` に `project.scripts` を設定し、`asciiplay` コマンドとしてインストール可能にする

### フェーズ4: 動作検証・調整
- 複数動画・複数解像度でのテスト
- ターミナルリサイズ時の表示崩れ確認
- `Ctrl+C` 中断時のカーソル復元確認

### フェーズ5: 拡張機能（任意）
- 音声同時再生の追加
- プレイリスト機能の追加
- 一時停止・キー入力操作の追加
- 高解像度表示モード（Braille文字）の追加

## 8. マイルストーン

| フェーズ | 内容 | 完了目安 |
|---|---|---|
| 1 | 単発スクリプトでの再生確認 | 動作するプロトタイプ完成 |
| 2 | 登録機能単体テスト完了 | JSON読み書き確認 |
| 3 | `asciiplay` コマンドとして統合完了 | インストール・実行確認 |
| 4 | 各種動画でのテスト完了 | 表示崩れ・中断処理の安定化 |
| 5 | 拡張機能の実装（任意） | 音声・プレイリスト等 |

## 9. 今後の検討事項

- フルカラー非対応ターミナルへのフォールバック（256色/16色モード）
- 大きい動画ファイルに対するパフォーマンス最適化（フレーム変換のボトルネック確認）
- 設定ファイルのバックアップ・複数マシン間での共有方法
```